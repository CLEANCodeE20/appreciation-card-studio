services:
  - type: site
    name: appreciation-card-studio
    runtime: static
    plan: free
    buildCommand: npm install && npm run build
    publishDir: dist
    envVars:
      - key: NODE_VERSION
        value: 20
    autoDeploy: false
