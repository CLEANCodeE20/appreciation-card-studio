services:
  - type: web
    name: appreciation-card-studio
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: 20
    autoDeploy: false
    healthCheckPath: /
